export type CurrencyHistoryParamsType = {
  period_id: string;
  time_start: string;
  time_end: string;
};

export type CurrencyHistoryFetchDataType = {
  currency: string;
  dateStart: string;
  dateEnd: string;
};
